import ContactForm from "./ContactForm";

const Contact = () => {
  return (
    <section id="contact" className="flex flex-col justify-center min-h-screen gap-4 p-8">
      <ContactForm />
    </section>
  );
};

export default Contact;
